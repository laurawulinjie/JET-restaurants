import { mount } from "@vue/test-utils";
import { createPinia, setActivePinia } from "pinia";
import CuisineFilter from "@/components/CuisineFilter.vue";

describe("CuisineFilter", () => {
  it.todo("should render correctly with given cuisines");
  it.todo("should not render when no cuisines are provided");
  it.todo("should toggles cuisine selection when button is clicked");
  it.todo("should active state for selected cuisines");
});
